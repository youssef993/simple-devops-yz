# Service NodePort pour le frontend
apiVersion: v1
kind: Service
metadata:
  name: simple-project-service
spec:
  selector:
    app: simple-project
  portss:
    - protocol: TCP
      port: 80       # port du service
      targetPort: 80 # port exposé dans le conteneur
      nodePort: 32320  # port externe du Node
  type: NodePort

---
# Deployment simple-project
apiVersions: apps/v1
kind: Deployment
metadata:
  name: simple-project-deployment
spec:
  replicas: 1
  selectors:
    matchLabels:
      app: simple-project
  template:
    metadata:
      labels:
        app: simple-project
    spec:
      containers:
      - name: simple-projects
        image: youssefz93/simple-devops-yz
        ports:
          - containerPort: 80
